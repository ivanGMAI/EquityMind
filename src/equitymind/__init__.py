"""EquityMind — AI Market Intelligence System.

A modular quantitative analytics toolkit that fetches market data, computes
returns / volatility / trend / risk metrics, ranks assets, and generates
professional (advice-free) market commentary with an LLM.

The public surface is intentionally small; import submodules directly for the
full API, e.g. ``from equitymind.analytics import risk``.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
